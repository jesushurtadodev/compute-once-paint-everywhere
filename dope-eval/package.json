{
  "name": "dope-eval",
  "version": "0.1.0",
  "type": "module",
  "description": "DOPE rung 5 — a scored eval harness for model-produced output. Deterministic-first, LLM-as-judge with required evidence, CI gate. Built on the Vercel AI SDK.",
  "bin": { "dope-eval": "run.js" },
  "scripts": {
    "eval": "node run.js"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.0",
    "ai": "^5.0.0",
    "zod": "^3.25.0"
  }
}
